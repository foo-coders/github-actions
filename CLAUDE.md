# CLAUDE.md

See [AGENTS.md](AGENTS.md) for repo conventions.

## Worktrees

When creating a worktree, pass a meaningful `name` that reflects the task at hand, instead of leaving it to generate a random one. Use a slash-style name with a conventional-commit prefix (e.g. `fix/release-please-pin`).
