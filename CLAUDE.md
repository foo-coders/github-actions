# CLAUDE.md

See [AGENTS.md](AGENTS.md) for repo conventions.

## Worktrees

The worktree `name` becomes the branch name: pass `<type>/<task-slug>`, where `<type>` is the conventional-commit type of the commits the work will produce (e.g. `fix/release-please-pin`).
