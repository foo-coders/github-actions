{
  "name": "github-actions",
  "private": true,
  "description": "Tooling for a collection of GitHub Actions",
  "scripts": {
    "format": "prettier --write .",
    "format:check": "prettier --check .",
    "lint": "actionlint",
    "install-dev-tools": "npm ci && bash install-actionlint.sh"
  },
  "devDependencies": {
    "prettier": "^3.9.6"
  }
}
